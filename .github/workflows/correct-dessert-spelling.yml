name: Correct dessert spelling

on:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  correct:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Correct soapapilla spelling
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          old = 'SOAPAPILLA'
          new = 'SOPAPILLA'
          count = s.count(old)
          if count < 1:
              raise SystemExit(f'Expected at least 1 occurrence of {old!r}, found {count}')
          p.write_text(s.replace(old, new), encoding='utf-8')
          PY
      - name: Commit and push
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'Correct sopapilla spelling'
          git push
